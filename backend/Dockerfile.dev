FROM node:20-alpine
WORKDIR /app

COPY package.json pnpm-lock.yaml ./
RUN npm install -g pnpm@10.20.0 && pnpm install --frozen-lockfile

EXPOSE 4000
# Код монтируется в контейнер томом; запуск через compose: npm run dev
